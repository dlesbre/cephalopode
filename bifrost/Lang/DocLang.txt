The Language Lang
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Lang==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Integer literals //Integer// are nonempty sequences of digits.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Lang are the following:
  | ``action`` | ``actiontype`` | ``alias`` | ``alwayson``
  | ``auto`` | ``by`` | ``call`` | ``combinational``
  | ``define`` | ``do`` | ``else`` | ``external``
  | ``fields`` | ``for`` | ``fourphase`` | ``goto``
  | ``if`` | ``inline`` | ``label`` | ``name``
  | ``off`` | ``on`` | ``power`` | ``powershake``
  | ``protocol`` | ``provided`` | ``pulseecho`` | ``reading``
  | ``return`` | ``state`` | ``subroutine`` | ``then``
  | ``twophase`` | ``type`` | ``validready`` | ``var``
  | ``via`` | ``while`` | ``writing`` |

The symbols used in Lang are the following:
  | ; | = | : | [
  | ] | , | => | ->
  | () | ( | ) | {
  | } | --%<-- | | | &
  | ^ | == | != | <
  | > | <= | >= | +
  | - | * | / | %
  | << | >> | |>> | ~
  | --> | . | :: | _

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of Lang==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[TopLevel]//
  | //[TopLevel]// | -> | **eps**
  |  |  **|**  | //TopLevel// //[TopLevel]//
  | //TopLevel// | -> | //Decl// ``;``
  |  |  **|**  | //SubDef//
  | //Decl// | -> | ``name`` //Ident//
  |  |  **|**  | ``protocol`` //Protocol//
  |  |  **|**  | ``state`` //[StateAspect]//
  |  |  **|**  | ``actiontype`` //ActionTypeName// ``=`` //FunPat// ``reading`` //StateAspects// ``writing`` //StateAspects//
  |  |  **|**  | ``action`` //ActionName// ``:`` //ActionTypeName// ``provided`` ``by`` //Provider// ``via`` //Protocol//
  |  |  **|**  | //VarDecl//
  |  |  **|**  | ``type`` //TypeName// //TypeCompileDefinition//
  |  |  **|**  | ``alias`` //TypeName// ``=`` //Type//
  |  |  **|**  | ``fields`` //TypeName// ``=`` ``[`` //[FieldDef]// ``]``
  |  |  **|**  | ``define`` //DefineName// //[DefineParam]// ``=`` //Exp//
  | //[FieldDef]// | -> | **eps**
  |  |  **|**  | //FieldDef//
  |  |  **|**  | //FieldDef// ``,`` //[FieldDef]//
  | //[DefineParam]// | -> | **eps**
  |  |  **|**  | //DefineParam// //[DefineParam]//
  | //VarDecl// | -> | ``var`` //Type1// //[VarName]//
  | //[VarName]// | -> | //VarName//
  |  |  **|**  | //VarName// ``,`` //[VarName]//
  | //StateAspects// | -> | ``[`` //[StateAspect]// ``]``
  | //[StateAspect]// | -> | **eps**
  |  |  **|**  | //StateAspect//
  |  |  **|**  | //StateAspect// ``,`` //[StateAspect]//
  | //Provider// | -> | ``external``
  |  |  **|**  | //String//
  | //Protocol// | -> | ``auto``
  |  |  **|**  | //CommProtocol// //PowerProtocol//
  | //CommProtocol// | -> | ``twophase``
  |  |  **|**  | ``fourphase``
  |  |  **|**  | ``pulseecho``
  |  |  **|**  | ``validready``
  |  |  **|**  | ``combinational``
  | //PowerProtocol// | -> | ``alwayson``
  |  |  **|**  | ``powershake``
  | //TypeCompileDefinition// | -> | ``=>`` //String//
  |  |  **|**  | **eps**
  | //FieldDef// | -> | //FieldName// ``:`` //Type//
  | //FieldName// | -> | //Ident//
  |  |  **|**  | //String//
  | //FunPat// | -> | //Pattern//
  |  |  **|**  | //Pattern// ``->`` //FunPat//
  | //Pattern// | -> | //Param//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Pattern// ``,`` //[Pattern]// ``)``
  | //[Pattern]// | -> | **eps**
  |  |  **|**  | //Pattern//
  |  |  **|**  | //Pattern// ``,`` //[Pattern]//
  | //Param// | -> | //Ident// ``:`` //Type1//
  | //Type// | -> | //Type1// ``->`` //Type//
  |  |  **|**  | //Type1//
  | //Type1// | -> | //Ident//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Type// ``,`` //[Type]// ``)``
  |  |  **|**  | ``(`` //Type// ``)``
  | //[Type]// | -> | //Type//
  |  |  **|**  | //Type// ``,`` //[Type]//
  | //SubDef// | -> | ``subroutine`` //[SubFlag]// //SubroutineName// ``:`` //FunPat// ``{`` //[SubLine]// ``}``
  | //[SubFlag]// | -> | **eps**
  |  |  **|**  | //SubFlag// //[SubFlag]//
  | //[SubLine]// | -> | **eps**
  |  |  **|**  | //SubLine// //[SubLine]//
  | //SubFlag// | -> | ``inline``
  | //SubLine// | -> | //VarDecl// ``;``
  |  |  **|**  | //Stm//
  | //Stm// | -> | //NormalStm// ``;``
  |  |  **|**  | //AbnormalStm//
  | //NormalStm// | -> | //Assignish//
  |  |  **|**  | ``return``
  |  |  **|**  | ``goto`` //GotoLabel//
  |  |  **|**  | ``power`` //Power// //ActionName//
  | //Power// | -> | ``on``
  |  |  **|**  | ``off``
  | //Assignish// | -> | //Exp// ``=`` //Exp//
  |  |  **|**  | //Exp//
  | //AbnormalStm// | -> | ``label`` //GotoLabel// ``:``
  |  |  **|**  | ``--%<--``
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //StmBlock// ``else`` //StmBlock//
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //StmBlock//
  |  |  **|**  | ``while`` ``(`` //Exp// ``)`` //StmBlock//
  |  |  **|**  | ``for`` ``(`` //Assignish// ``;`` //Exp// ``;`` //Assignish// ``)`` //StmBlock//
  |  |  **|**  | //StmBlock//
  | //StmBlock// | -> | ``{`` //[Stm]// ``}``
  | //[Stm]// | -> | **eps**
  |  |  **|**  | //Stm// //[Stm]//
  | //StateAspect// | -> | //Ident//
  | //TypeName// | -> | //Ident//
  | //VarName// | -> | //Ident//
  | //ActionTypeName// | -> | //Ident//
  | //ActionName// | -> | //Ident//
  | //DefineName// | -> | //Ident//
  | //DefineParam// | -> | //Ident//
  | //SubroutineName// | -> | //Ident//
  | //GotoLabel// | -> | //Ident//
  | //Call// | -> | ``do`` //ActionName// //Args//
  |  |  **|**  | ``call`` //SubroutineName// //Args//
  | //Args// | -> | //[Exp10]//
  | //[Exp10]// | -> | **eps**
  |  |  **|**  | //Exp10// //[Exp10]//
  | //Exp// | -> | ``if`` //Exp// ``then`` //Exp// ``else`` //Exp//
  |  |  **|**  | //Exp1//
  | //Exp1// | -> | //Exp1// ``|`` //Exp2//
  |  |  **|**  | //Exp2//
  | //Exp2// | -> | //Exp2// ``&`` //Exp3//
  |  |  **|**  | //Exp2// ``^`` //Exp3//
  |  |  **|**  | //Exp3//
  | //Exp3// | -> | //Exp4// ``==`` //Exp4//
  |  |  **|**  | //Exp4// ``!=`` //Exp4//
  |  |  **|**  | //Exp4// ``<`` //Exp4//
  |  |  **|**  | //Exp4// ``>`` //Exp4//
  |  |  **|**  | //Exp4// ``<=`` //Exp4//
  |  |  **|**  | //Exp4// ``>=`` //Exp4//
  |  |  **|**  | //Exp4//
  | //Exp4// | -> | //Exp4// ``+`` //Exp5//
  |  |  **|**  | //Exp4// ``-`` //Exp5//
  |  |  **|**  | //Exp5//
  | //Exp5// | -> | //Exp5// ``*`` //Exp6//
  |  |  **|**  | //Exp5// ``/`` //Exp6//
  |  |  **|**  | //Exp5// ``%`` //Exp6//
  |  |  **|**  | //Exp6//
  | //Exp6// | -> | //Exp6// ``<<`` //Exp7//
  |  |  **|**  | //Exp6// ``>>`` //Exp7//
  |  |  **|**  | //Exp6// ``|>>`` //Exp7//
  |  |  **|**  | //Exp7//
  | //Exp7// | -> | ``~`` //Exp7//
  |  |  **|**  | //Exp8//
  | //Exp8// | -> | //Call//
  |  |  **|**  | //Exp9//
  | //Exp9// | -> | //Exp9// //Exp10//
  |  |  **|**  | //Exp10//
  | //Exp10// | -> | //VarName//
  |  |  **|**  | //Integer//
  |  |  **|**  | //String//
  |  |  **|**  | //Exp10// ``-->`` //FieldName//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Exp// ``,`` //[Exp]// ``)``
  |  |  **|**  | //Exp10// ``.`` //Integer//
  |  |  **|**  | ``(`` //Exp10// ``::`` //Type// ``)``
  |  |  **|**  | ``_``
  |  |  **|**  | //Exp11//
  | //Exp11// | -> | ``(`` //Exp// ``)``
  | //[Exp]// | -> | //Exp//
  |  |  **|**  | //Exp// ``,`` //[Exp]//


